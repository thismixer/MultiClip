pkgname=mclip
pkgver=1.0.0
pkgrel=1
pkgdesc="Clipboard sync between Mac and Arch"
arch=('x86_64')
url="https://github.com/thismixer/MultiClip"
license=('MIT')
depends=('wl-clipboard' 'xclip')
package() {
  install -Dm755 "${srcdir}/mclip" "${pkgdir}/usr/bin/mclip"
}